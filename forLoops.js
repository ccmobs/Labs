//object example with a method property
const mobley = {
    firstName: 'Chris',
    lastName: 'Mobley',
    birthYeah: 1957,
    job: 'designer, creator',
    friends: ['Michael', 'Peter', 'Steven'],
    hasDriversLicense: true,
  
    //method property
    // calcAge: function (birthYear) {
    //   return 2022 - birthYear;
    // }
    };


    // console.log(mobley.calcAge(1957));
    let numbs = [1,2,3];

    //FOR LOOP: initialize ; condition; counter
    for(i = 0; i < numbs.length; i++) {
        console.log(numbs[i]);
    };

   const fruits = ['apples', 'pears', 'bananas'];
   const fruits2 = [];
   for(i = 0; i < fruits.length; i++) {
    fruits2.push(fruits[i]);
   }

  console.log(fruits);
